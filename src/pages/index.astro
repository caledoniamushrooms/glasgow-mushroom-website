---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Glasgow Mushroom Company" showNav={false} showFooter={false}>
  <section class="splash">
    <div class="splash__content">
      <img src="/images/logo.svg" alt="Glasgow Mushroom Company" class="splash__logo" />
      <p class="splash__tagline">Glasgow Mushroom Co.</p>
      <a href="/home" class="splash__enter">Enter</a>
    </div>
  </section>
</BaseLayout>

<style>
  .splash {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background-image: url('/images/splash-hero.jpg');
    background-size: cover;
    background-position: center;
  }

  .splash__content {
    text-align: center;
    padding: 2rem;
  }

  .splash__logo {
    width: 250px;
    height: auto;
    margin-bottom: 0.5rem;
  }

  .splash__tagline {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: 2rem;
    letter-spacing: 0.02em;
  }

  .splash__enter {
    display: inline-block;
    font-family: var(--font-heading);
    font-size: 22px;
    font-weight: 700;
    color: #000;
    background-color: #fff;
    padding: 9px 10px;
    border: none;
    border-radius: 6px;
    transition: opacity 0.2s ease;
  }

  .splash__enter:hover {
    opacity: 0.85;
  }

  @media (min-width: 768px) {
    .splash__logo {
      width: 500px;
    }

    .splash__tagline {
      font-size: 2rem;
    }
  }
</style>
